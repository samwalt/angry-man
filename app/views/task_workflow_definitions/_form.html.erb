<%= form_for(@task_workflow_definition) do |f| %>
  <% if @task_workflow_definition.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_workflow_definition.errors.count, "error") %> prohibited this task_workflow_definition from being saved:</h2>

      <ul>
      <% @task_workflow_definition.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :model_name %><br>
    <%= f.text_field :model_name %>
  </div>
  <div class="field">
    <%= f.label :diagram_generated %><br>
    <%= f.check_box :diagram_generated %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
